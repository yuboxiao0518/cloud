eureka:
  instance:
    hostname: eureka1
  client:
    #fetch-registry: true
    #register-with-eureka: true
    service-url:
      defaultZone: http://eureka2:8762/eureka/
spring:
  application:
    name: eureka-server
server:
  port: 8761